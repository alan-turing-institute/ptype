# ptype build script notes

- see `build.yml` for example usage
- scripts should be run from package root
- `do-` scripts run Python build steps inside a `virtualenv`; path to Python binary must be supplied as `$1`
