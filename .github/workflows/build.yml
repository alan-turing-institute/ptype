name: build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v1
      with:
        python-version: '3.8'

    - name: Build
      run: |
        cd build
        # TODO: use "which python" to extract this path
        ./build.sh /Users/runner/hostedtoolcache/Python/3.8.2/x64/bin/python
